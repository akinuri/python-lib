
0. git status

1. stage the changes
2. dump the changes
   a. git diff --name-status HEAD~1 > changes.txt
3. analyze the output
4. copy added and modified files
5. log deleted and renamed files
6. upload


----------------------------------------

Shows modified and deleted files:

git diff --name-status
git diff --name-status HEAD~1
git diff --name-status HEAD~1..HEAD

git status --short
git status --porcelain

----------------------------------------

Tags:

A   added
C   copied
D   deleted
M   modified
R   renamed
T   ??
U   unmerged
X   unknown
B   broken

----------------------------------------

Lists files:

git ls-files --deleted
git ls-files --modified

------------------------------

Lists untracked files:

git ls-files --others --exclude-standard


